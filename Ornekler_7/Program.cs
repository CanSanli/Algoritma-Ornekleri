using System;

namespace Ornekler_7
{
    class Program
    {
        static void Main(string[] args)
        {
            #region longest common sub sequence
            //string s1 = "777777a1b2c33d444e55f66gh";
            //string s2 = "axjbdch";
            //int[,] subsequence = new int[s1.Length, s2.Length];
            //int adet = 0;
            //for (int i = 0; i < s1.Length; i++)
            //{
            //    for (int j = 0; j < s2.Length; j++)
            //    {
            //        if (s1[i] == s2[j])
            //        {
            //            if (i == 0 || j == 0) subsequence[i, j] = 1;
            //            else subsequence[i, j] = subsequence[i - 1, j - 1] + 1;
            //            if (subsequence[i, j] > adet) adet = subsequence[i, j];
            //        }
            //        else
            //        {
            //            if (i == 0 || j == 0)
            //            {
            //                if (i == 0 && j == 0) subsequence[i, j] = 0;
            //                else
            //                {
            //                    if (i == 0) subsequence[i, j] = subsequence[i, j - 1];
            //                    if (j == 0) subsequence[i, j] = subsequence[i - 1, j];
            //                }
            //            }
            //            else
            //            {
            //                subsequence[i, j] = Max(subsequence[i - 1, j], subsequence[i, j - 1]);
            //                //if (subsequence[i - 1, j] > subsequence[i, j - 1]) subsequence[i, j] = subsequence[i - 1, j];
            //                //else
            //                //{
            //                //    subsequence[i, j] = subsequence[i, j - 1];
            //                //}
            //            }
            //        }
            //    }
            //}
            //Console.WriteLine(adet);
            #endregion
        }
    }
}
